describe('Add 2 and 2', () => {
  it('2 plus 2 should be 4', () => {
    // arrange
    const result = 4;

    // act
    const answer = 2 + 2;
    // assert

    expect(answer).toBe(result);
  });
});
