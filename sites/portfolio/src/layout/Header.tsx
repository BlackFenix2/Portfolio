import * as React from 'react';
import { Typography } from '@material-ui/core';

const Head = () => {
  return (
    <Typography variant="h6" noWrap>
      Ernie Francis
    </Typography>
  );
};

export default Head;
