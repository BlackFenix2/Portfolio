import * as React from 'react';
import { Typography } from '@mui/material';

const Head = () => {
  return (
    <Typography variant="h6" noWrap>
      Ernie Francis
    </Typography>
  );
};

export default Head;
