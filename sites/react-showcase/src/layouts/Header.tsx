import * as React from 'react';
import { Typography } from '@material-ui/core';

const Head = () => {
  return (
    <Typography variant="h6" noWrap>
      React Showcase
    </Typography>
  );
};

export default Head;
