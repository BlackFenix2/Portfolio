{
  "name": "root-app",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "heroku-postbuild": "cd client && yarn install && yarn run build",
    "start": "yarn start:client && yarn start:server",
    "start:client": "cd client && yarn install && yarn start",
    "start:server": "cd server && dotnet restore && dotnet run"
  }
}
