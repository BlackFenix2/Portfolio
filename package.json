{
  "name": "monorepo",
  "private": true,
  "version": "0.0.0",
  "license": "GNU",
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "scripts": {
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "update": "yarn upgrade-interactive --latest"
  },
  "devDependencies": {
    "eslint-config-core": "*",
    "prettier": "^1.19.1",
    "typescript": "^3.7.5"
  }
}
